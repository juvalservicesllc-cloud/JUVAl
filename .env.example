# Juval — variables de entorno (plantilla, sin valores reales)
#
# Copiar a .env (ignorado por Git, ver .gitignore) y completar con
# valores reales SOLO en el entorno donde corran los procesos que los
# necesitan. Nunca commitear un .env con valores reales.

# --- interfaces/api (backend FastAPI, Fase 4A) -----------------------

# Directorio base para archivos temporales de una corrida (Excel subido
# + Excel generado). Opcional -- si no se define, usa el directorio
# temporal del sistema. Ver docs/architecture/API_CONTRACT.md §6.
JUVAL_RUN_STORAGE_DIR=

# Límite técnico de tamaño de archivo subido, en bytes. Opcional -- si
# no se define, no se impone ningún límite (mismo comportamiento que el
# Core ya tenía). El valor comercial definitivo sigue PENDIENTE, ver
# docs/architecture/SECURITY.md §3 y API_CONTRACT.md §4. NO copiar aquí
# un número inventado.
JUVAL_MAX_UPLOAD_BYTES=

# Orígenes permitidos para CORS, separados por coma. Vacío por defecto
# (ningún origen permitido). Nunca usar "*". En Railway (ADR-018),
# configurar con la URL real que Vercel asigne al frontend una vez
# desplegado -- esa URL no existe todavía, no inventarla aquí.
JUVAL_CORS_ORIGINS=

# NO establecer manualmente en Railway -- Railway inyecta PORT
# automáticamente y railway.toml::startCommand ya lo usa
# (--port $PORT). Documentado aquí solo para que no se confunda con
# una variable que el operador deba configurar.
# PORT=(gestionado por Railway)

# Selector explícito de ExecutionRunStore (ADR-017), valores válidos:
# "sqlite" | "supabase". Es la fuente de verdad cuando está definida --
# nunca se infiere del store a partir de qué variable de conexión está
# presente (así una JUVAL_SUPABASE_DB_URL o JUVAL_EXECUTION_DB_PATH
# heredada en un .env de desarrollo no puede redirigir en silencio la
# selección). Un valor fuera de {"sqlite","supabase"}, o la ausencia de
# la variable de conexión que el modo elegido requiere, falla
# explícitamente (ver interfaces/api/main.py::_execution_run_store).
# Sin definir, se mantiene el comportamiento legacy: SqliteExecutionRunStore
# si JUVAL_EXECUTION_DB_PATH está definida, si no, sin store configurado.
# Producción (Railway, ADR-018) NO debe depender del modo legacy --
# debe fijar JUVAL_EXECUTION_STORE=supabase explícitamente.
JUVAL_EXECUTION_STORE=

# Ruta a un archivo SQLite para persistir ExecutionRun localmente
# (ADR-013). Requerida cuando JUVAL_EXECUTION_STORE=sqlite, u opcional
# en modo legacy (JUVAL_EXECUTION_STORE sin definir) para invocar la
# API con persist=true en desarrollo/local.
JUVAL_EXECUTION_DB_PATH=

# --- Identidad humana / autorización (RF-03 backend, RF-04) ---------
#
# Proveedor: FusionAuth self-hosted (ADR-028 proveedor, ADR-031 alojamiento).
# ADR-022 (Okta) quedó RECHAZADA -- no usar su formato como referencia.

# Modo de autenticación del backend: "disabled" | "oidc".
# Sin definir = "disabled": TODOS los endpoints quedan SIN autenticar.
# Eso es aceptable solo en desarrollo local. **Producción DEBE fijar
# JUVAL_AUTH_MODE=oidc** -- un despliegue con "disabled" no puede
# satisfacer los hallazgos RF-03/RF-04 de Amazon. Un valor distinto de
# esos dos falla al arrancar (nunca degrada a sin autenticar).
JUVAL_AUTH_MODE=

# Emisor OIDC esperado (claim `iss`). Requerido cuando el modo es "oidc".
# El proveedor aprobado es FusionAuth (ADR-028); su emisor es la URL del
# tenant, p. ej. https://idp.example.com. Okta quedó RECHAZADO (ADR-022,
# 2026-08-19) -- no usar su formato como referencia.
# No inventar un valor aquí: el tenant de producción no existe todavía.
JUVAL_OIDC_ISSUER=

# Audiencia esperada (claim `aud`). Requerida cuando el modo es "oidc".
JUVAL_OIDC_AUDIENCE=

# URI del JWKS del emisor. Opcional -- si no se define se deriva como
# <issuer>/.well-known/jwks.json, que es la convención de OpenID Connect
# Discovery 1.0 §4 y la que publica FusionAuth. Definirla explícitamente
# solo con un proveedor que se aparte de esa convención.
JUVAL_OIDC_JWKS_URI=

# Claim del token que transporta los roles. Opcional, por defecto "roles".
# Roles reconocidos: viewer | operator | admin (ver interfaces/api/auth.py).
# Un rol no reconocido no otorga ningún permiso -- nunca acceso total.
JUVAL_OIDC_ROLES_CLAIM=

# NOTA: aquí no va ningún secreto. El backend valida tokens con la clave
# PÚBLICA del emisor (JWKS); no necesita client secret para eso.

# --- Supabase / PostgreSQL (persistencia de producción, ADR-017) -----
# Proyecto real ya verificado -- ver docs/architecture/SUPABASE.md §1.

# Pública (segura de exponer al frontend) -- URL del proyecto Supabase.
SUPABASE_URL=

# Pública (segura de exponer al frontend) -- protegida por Row Level
# Security (ver supabase/migrations/20260817000000_execution_runs.sql).
SUPABASE_ANON_KEY=

# SECRETA -- SOLO backend. Nunca en un build de frontend (nunca con
# prefijo VITE_ ni equivalente). Nunca en Git.
SUPABASE_SERVICE_ROLE_KEY=

# Connection string de Postgres para SupabaseExecutionRunStore
# (ADR-017). Requerida cuando JUVAL_EXECUTION_STORE=supabase. SOLO
# backend. Nunca en Git. Usar el Connection Pooler de Supabase
# (host tipo aws-0-<region>.pooler.supabase.com), no el host de
# conexión directa -- el host directo resuelve solo por IPv6 en
# proyectos nuevos, ver docs/architecture/SUPABASE.md §1.
JUVAL_SUPABASE_DB_URL=

# --- Frontend (React + Vite, Fase 4B -- NO implementado todavía) -----
# Documentado aquí como referencia de qué NO debe contener nunca una
# service role key -- ninguna variable VITE_* debe llevar secretos de
# backend, ver docs/architecture/SUPABASE.md §5.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_BASE_URL=

# --- BFF de navegador (ADR-034) --------------------------------------
#
# El navegador NUNCA recibe un access token, un refresh token, el
# code_verifier de PKCE ni el `state`. Todo eso vive en la sesión
# server-side; el navegador sólo lleva una cookie opaca HttpOnly.
# Estas variables sólo se leen cuando JUVAL_AUTH_MODE=oidc.

# client_id de la aplicación en FusionAuth. Requerido en modo "oidc".
JUVAL_OIDC_CLIENT_ID=

# client secret. El BFF es un cliente confidencial, así que normalmente
# existe. NUNCA se commitea: se inyecta como variable de entorno en
# Railway (docs/compliance/SECRETS.md).
JUVAL_OIDC_CLIENT_SECRET=

# Redirect URI EXACTA, registrada igual en FusionAuth. Se guarda en
# configuración a propósito: derivarla de la cabecera Host permitiría que
# una cabecera falsificada cambiara a dónde se envía el código.
# p. ej. https://api.example.com/api/v1/auth/callback
JUVAL_BFF_REDIRECT_URI=

# A dónde vuelve el navegador tras login / logout. Sólo rutas absolutas
# del mismo sitio; cualquier cosa con esquema o autoridad se ignora.
JUVAL_BFF_POST_LOGIN_REDIRECT=/
JUVAL_BFF_POST_LOGOUT_REDIRECT=/

# Endpoints OAuth2. Opcionales: por defecto se derivan del emisor con las
# rutas que publica FusionAuth. Definirlos sólo si el proveedor se desvía.
JUVAL_OIDC_AUTHORIZATION_ENDPOINT=
JUVAL_OIDC_TOKEN_ENDPOINT=
JUVAL_OIDC_END_SESSION_ENDPOINT=

# Desactiva el flag `Secure` de las cookies. SÓLO desarrollo local sobre
# HTTP, y sólo con este valor exacto: cualquier otro deja `Secure` puesto,
# de forma que un error tipográfico no puede degradarlo en producción.
# JUVAL_BFF_INSECURE_COOKIES=yes-local-http-only

# Un despliegue puede ser legítimamente "sólo bearer": `auth.py` valida
# tokens emitidos en otro sitio y ningún humano inicia sesión en este
# proceso. En esa forma NINGUNA de las variables de arriba se define, las
# rutas del BFF responden 404 y no hace falta almacén de sesiones. Definir
# SÓLO ALGUNAS es el estado peligroso: el arranque falla a propósito.

# --- Almacén de sesiones del BFF (ADR-036) ---------------------------
#
# El BFF dio estado de sesión al backend, que antes era sin estado. Railway
# puede correr varias instancias: una sesión creada en A debe leerse en B,
# y un login iniciado en A debe poder completarse por un callback que
# aterrice en B. Sin almacén compartido fallan las dos cosas.
#
# Se valida al ARRANCAR (lifespan de FastAPI, interfaces/api/main.py). Una
# configuración inválida impide arrancar; no hay degradación silenciosa a
# memoria y no hay fallback.

# "postgres" (por defecto) | "memory". Producción es siempre "postgres".
JUVAL_SESSION_STORE=

# DSN PostgreSQL para las tablas identity_sessions /
# identity_oauth_transactions. Si no se define, se usa JUVAL_SUPABASE_DB_URL.
# Es una cadena de conexión con contraseña: secreto de despliegue, nunca
# se commitea. Ver docs/compliance/SECRETS.md.
JUVAL_SESSION_DB_URL=

# Llavero de cifrado de tokens OAuth en reposo (AES-256-GCM).
# Formato: <key_id>:<clave de 32 bytes en base64>[,<key_id>:<clave>...]
# La PRIMERA entrada es la primaria y cifra; todas descifran, que es lo que
# hace aditiva la rotación. Genérala con un CSPRNG, p. ej.
#   python -c "import base64,os;print('k1:'+base64.b64encode(os.urandom(32)).decode())"
# NUNCA en el repositorio, NUNCA en la base de datos, NUNCA en una variable
# VITE_* (ésas se compilan dentro del bundle del navegador). Perderla
# equivale a cerrar todas las sesiones abiertas, no a perder datos.
JUVAL_SESSION_ENCRYPTION_KEYS=

# Opt-in explícito para usar sesiones en memoria con JUVAL_AUTH_MODE=oidc.
# Es una frase y no un booleano a propósito: nadie la pone por accidente y
# no se llega a ella copiando un "true" de otra variable. Sólo desarrollo
# local de un solo proceso; en producción está prohibido (ADR-036).
# JUVAL_SESSION_STORE_MEMORY_CONFIRM=yes-single-process-development-only
