# JUVAl frontend template. Copy to .env.local (ignored by Git).
# Every VITE_* value is embedded in the public browser bundle: never use secrets.

# Public URL of the FastAPI backend.
VITE_API_BASE_URL=http://localhost:8000
