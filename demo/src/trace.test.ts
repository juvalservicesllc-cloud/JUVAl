import{describe,expect,it}from"vitest";import{normalize,parseCsv}from"./engine";import{productTraces}from"./trace";const r=normalize(parseCsv("position-relative href,img-fluid src,product-brand-name,link,item-price,item-price (2)\n/a,,Brand,Title,$0.00,$20"))[0];describe("field traces",()=>{it("keeps source raw values and zero",()=>{const t=productTraces(r).find(x=>x.field==="supplier_cost")!;expect(t.kind).toBe("SOURCE");expect(t).toMatchObject({sourceColumn:"item-price",raw:"$0.00",value:"0",provenance:"VERIFIED_SOURCE"})});it("labels demo and calculated values",()=>{const traces=productTraces(r);expect(traces.find(x=>x.field==="selling_price")).toMatchObject({kind:"DEMO",origin:"DemoAmazonEnrichmentProvider"});for(const name of["profit","ROI","margin"])expect(traces.find(x=>x.field===name)).toMatchObject({kind:"CALCULATED",derivation:"CALCULATED_FROM_DEMO_DATA"})});it("keeps not-found demo traces explicit",()=>expect(productTraces({...r,weight:null}).find(x=>x.field==="weight")).toMatchObject({kind:"DEMO",value:"NOT_FOUND",provenance:"NOT_FOUND"}));it("never upgrades generated data or calculated results to supplier verification",()=>{const traces=productTraces(r);for(const name of["amazon_asin","amazon_title","selling_price","referral_fee","shipping","weight","hazmat","bulky"])expect(traces.find(x=>x.field===name)).not.toMatchObject({provenance:"VERIFIED_SOURCE"});for(const name of["profit","ROI","margin"])expect(traces.find(x=>x.field===name)).toMatchObject({derivation:"CALCULATED_FROM_DEMO_DATA"})})})
