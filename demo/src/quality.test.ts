import{describe,expect,it}from"vitest";import{normalize,parseCsv}from"./engine";import{dataQuality}from"./quality";const base=normalize(parseCsv("position-relative href,img-fluid src,product-brand-name,link,item-price,item-price (2)\n/a,img.jpg,Brand,Title,$10,$20"))[0];describe("factual data quality",()=>{it("reports source omissions and invalidity",()=>{const q=dataQuality({...base,url:"",image:"",brand:"Unknown",title:"Untitled supplier item",cost:null,suggested:null,raw:{...base.raw,"position-relative href":"","img-fluid src":"","product-brand-name":"",link:"","item-price":"","item-price (2)":"bad"}});expect(q.facts.map(x=>x.code)).toEqual(expect.arrayContaining(["MISSING_supplier_url","MISSING_image_url","MISSING_brand","MISSING_supplier_title","MISSING_supplier_cost","INVALID_suggested_price"]))});it("reports demo uncertainty once per semantic condition",()=>{const q=dataQuality({...base,match:"NOT_FOUND",weight:null,hazmat:"UNKNOWN",bulky:"UNKNOWN"});expect(q.facts.map(x=>x.code)).toEqual(expect.arrayContaining(["AMAZON_NOT_FOUND","WEIGHT_NOT_FOUND","HAZMAT_UNKNOWN","BULKY_UNKNOWN"]));expect(q.facts.filter(x=>x.code==="AMAZON_NOT_FOUND")).toHaveLength(1)});it("does not classify calculated values as source",()=>{const q=dataQuality(base);expect(q.groups.VERIFIED_SOURCE).not.toContain("Profit");expect(q.groups.VERIFIED_SOURCE).toContain("Supplier cost")})})
