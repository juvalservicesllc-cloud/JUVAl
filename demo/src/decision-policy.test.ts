import{describe,expect,it}from"vitest";import{applyDecisionPolicy,defaultDecisionPolicy,decideDemo,validDecisionPolicy}from"./demo-engine";import{normalize,parseCsv}from"./engine";const base={profit:20,roi:.3,match:"DEMO_FIXTURE" as const,hazmat:"ABSENT",bulky:"ABSENT"};
describe("configurable demo ROI policy",()=>{it("preserves default semantics and allows a custom ROI policy",()=>{expect(decideDemo(base).decision).toBe("REVIEW");expect(decideDemo(base,{...defaultDecisionPolicy,reviewRoiThreshold:.25,buyRoiThreshold:.3}).decision).toBe("BUY")});it("keeps PASS blockers ahead of custom thresholds",()=>{const policy={...defaultDecisionPolicy,reviewRoiThreshold:0,buyRoiThreshold:0};expect(decideDemo({...base,hazmat:"PRESENT"},policy).decision).toBe("PASS");expect(decideDemo({...base,match:"NOT_FOUND"},policy).decision).toBe("PASS")});it("rejects invalid threshold ordering and applies a policy to saved records",()=>{expect(validDecisionPolicy({...defaultDecisionPolicy,reviewRoiThreshold:.5,buyRoiThreshold:.25})).toBe(false);const record=normalize(parseCsv("position-relative href,img-fluid src,product-brand-name,link,item-price,item-price (2)\n/a,,Brand,Title,$10,$20"))[0];expect(applyDecisionPolicy([record],defaultDecisionPolicy)[0].recordRef).toBe(record.recordRef)})});
