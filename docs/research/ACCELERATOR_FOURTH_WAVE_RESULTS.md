# Accelerator fourth wave — 2026-09-10

Status: PARTIALLY IMPLEMENTED; public identity and Amazon readiness remain blocked.
Starting HEAD: 98d3ef987af604919d49ac1f1991426d174d6fae, authenticated SSH fetch
confirmed origin/master equal and 0/0 before changes. No transport change.

## Current behavioral evidence

- Backend: 839 PASS / 43 SKIP; one existing Starlette/httpx deprecation warning.
  Real disposable nginx was available (downloaded package extracted privately,
  not installed). N-1 and server_tokens assertions passed in this suite.
- PostgreSQL: 53 PASS / 2 memory-only SKIP, plus actual private-cluster restart.
  New persisted additive-key-rotation test proves old-key retention, rewrite
  under new key, and fail-closed removal of still-needed keys. No production
  DSN, key, table or auth configuration changed. Temporary cluster removed.
- Browser site semantics lab: PASS for same-site fetch, cross-site exclusion,
  host-only/HttpOnly scope and top-level navigation. Not public TLS evidence.
- Portal: 12 model + 4 component + 3 exporter tests PASS, lint/build PASS.
  E2E not rerun this wave. Frontend tests not rerun: frozen trees unchanged.
- Compliance: 9 PASS / 1 WARN / 0 FAIL; security owner assignment remains open.
  Secret scan before report addition: 477 tracked files, no matches.
- Four npm lockfile audits (frontend, frontend-next, demo, portal): zero local
  advisories each. Installed Python audit: zero local advisories. These do NOT
  identify or close the six operator-reported GitHub alerts (4 high/2 moderate).

## Boundaries

`gh --version` and `gh auth status`: command unavailable.
DEPENDABOT_METADATA = BLOCKED_BY_GITHUB_AUTHORIZATION
DEPENDENCY_SECURITY_STATUS = PARTIAL / GITHUB_METADATA_REQUIRED
No frontend security change is justified without specific advisory evidence.

A fresh operator-visible Chromium context on local display :99 presents the
FusionAuth Admin authentication page. It is intentionally retained for private
authentication and continuation; no cookies, credentials or screenshots were
extracted. This is an operator handoff, not authenticated Admin verification.
The exact tenant/client existence controls were rerun successfully; names and
roles were not reverified. fusionauth-app.service and PostgreSQL are active.

REAL_LOGIN_STATUS = NOT_VERIFIED
REDIRECT_BASELINE_STATUS = NOT_ESTABLISHED
TEMP_REDIRECT_ADD_STATUS = NOT_EXECUTED
TEMP_REDIRECT_CLEANUP_STATUS = NOT_APPLICABLE_NO_MUTATION
ORIGINAL_REDIRECT_RESTORED = NOT_APPLICABLE_NO_MUTATION
REAL_LOGIN_SURFACE_INVENTORY = NOT_OBSERVED; generic evidence remains separate.
D1_STATUS = BLOCKED_BY_REAL_LOGIN_EVIDENCE
N1_STATUS = REMEDIATED / LAB_BEHAVIOURALLY_VERIFIED
N3_STATUS = server_tokens off LAB_BEHAVIOURALLY_VERIFIED; other headers await
actual hosted UI/MIME/MFA compatibility. No CSP/HSTS/rate-limit activation.
NGINX_FUSIONAUTH_STATUS = NOT_VERIFIED (echo-upstream lab is separate).

ADR-038 remains ACCEPTED: same-site app/api/id subdomains under one owned
registrable domain. HUMAN_DOMAIN_SELECTION_PENDING. Domain-independent cookie,
CSRF/CORS, issuer, logout and TLS boundary preparation remains in that ADR.
SESSION_STATUS = IMPLEMENTED_TESTED_NOT_ACTIVATED. Live migration offline
preflight rerun: exact up/down hashes produced, database NOT_CHECKED; target,
owner, restore evidence and production permission still required.
RF03_HUMAN_RUNBOOK_READY = YES; forced password change and .invalid-only
disposable email clarified. Delivery-dependent cases BLOCKED without separately
approved isolated delivery. RF03 execution NOT_AUTHORIZED.
CONTROL6_STATUS = PARTIALLY_SATISFIED.
BACKUP_READINESS = on-host service last result success; off-host proposal ready,
destination/custody/owner pending. A successful backup exit is not restore proof.

## Human checkpoint and continuation

1. Authenticate privately in the visible FusionAuth Admin window; then CONTINUE.
2. Provide authorized GitHub alert metadata access separately (no secret values).
3. Select owned domain, security owner and off-host recovery destination/custody.
4. Later separately authorize the reviewed live migration and RF03 procedure.

After authentication: baseline complete redirect configuration, add only the
authorized loopback callback, render with ephemeral PKCE, inventory paths,
restore exact baseline/read back, then resolve D-1 and run real-upstream nginx
integration. No mutation or claimed restoration has occurred yet.

Self-review: tests exercise persisted behavior; no architecture or production
policy changed; no frontend changes; no temporary redirect exists from this
wave. Downloaded nginx and private DB scratch removed. The private Admin browser
is deliberately pending handoff. Ponytail unavailable; manual simplicity review
used. Progress must be generated by ADR-040 at final committed HEAD, not inferred
from these test counts.
